<UserControl x:Class="GitUI.Views.Tabs.WatchTab"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:GitUI.ViewModels.Tabs"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=vm:WatchTabViewModel}"
             mc:Ignorable="d">
    <Grid Margin="0,16,0,0">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Border Grid.Row="0"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                CornerRadius="10" Padding="20" Margin="0,0,0,16"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1">
            <StackPanel>
                <TextBlock Text="자동 폴더 감시 (풀오토)" FontSize="16" FontWeight="SemiBold" Margin="0,0,0,4"/>
                <TextBlock TextWrapping="Wrap" Opacity="0.6" FontSize="12" Margin="0,0,0,12">
                    <Run Text="감시 시작하면 창을 닫아도 트레이에서 계속 동작합니다."/>
                    <LineBreak/>
                    <Run Text="앱 종료 후 다시 켜도, Windows 재부팅 후에도 자동으로 재개됩니다."/>
                </TextBlock>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="*"/>
                        <ColumnDefinition Width="Auto"/>
                    </Grid.ColumnDefinitions>
                    <TextBox Grid.Column="0"
                             Text="{Binding SelectedFolder, UpdateSourceTrigger=PropertyChanged}"
                             IsReadOnly="True"
                             ui:ControlHelper.PlaceholderText="폴더가 선택되지 않음"/>
                    <Button Grid.Column="1" Command="{Binding PickFolderCommand}"
                            Content="폴더 선택" Margin="8,0,0,0"
                            IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolToVis}}"/>
                </Grid>

                <Grid Margin="0,0,0,12">
                    <Grid.ColumnDefinitions>
                        <ColumnDefinition Width="2*"/>
                        <ColumnDefinition Width="*"/>
                    </Grid.ColumnDefinitions>
                    <StackPanel Grid.Column="0" Margin="0,0,8,0">
                        <TextBlock Text="커밋 메시지" Opacity="0.75" FontSize="12" Margin="0,0,0,4"/>
                        <TextBox Text="{Binding CommitMessage, UpdateSourceTrigger=PropertyChanged}"/>
                    </StackPanel>
                    <StackPanel Grid.Column="1" Margin="8,0,0,0">
                        <TextBlock Text="디바운스 (초)" Opacity="0.75" FontSize="12" Margin="0,0,0,4"/>
                        <ui:NumberBox Value="{Binding DebounceSeconds}" Minimum="1" Maximum="300"
                                      SpinButtonPlacementMode="Inline"/>
                    </StackPanel>
                </Grid>

                <CheckBox IsChecked="{Binding MirrorDeletions}"
                          Margin="0,0,0,8"
                          IsEnabled="{Binding IsRunning, Converter={StaticResource InverseBoolEnableConverter}}">
                    <StackPanel>
                        <TextBlock Text="삭제도 미러링 (로컬에서 사라진 파일 → 원격에서도 삭제)"/>
                        <TextBlock Text="끄면 추가/수정만 푸시 (안전)" Opacity="0.5" FontSize="10"/>
                    </StackPanel>
                </CheckBox>

                <Button Command="{Binding ToggleWatchCommand}"
                        Style="{StaticResource AccentButtonStyle}"
                        Height="40">
                    <TextBlock>
                        <TextBlock.Style>
                            <Style TargetType="TextBlock">
                                <Setter Property="Text" Value="감시 시작"/>
                                <Style.Triggers>
                                    <DataTrigger Binding="{Binding IsRunning}" Value="True">
                                        <Setter Property="Text" Value="감시 중지"/>
                                    </DataTrigger>
                                </Style.Triggers>
                            </Style>
                        </TextBlock.Style>
                    </TextBlock>
                </Button>

                <TextBlock Text="{Binding StatusMessage}"
                           Visibility="{Binding StatusMessage, Converter={StaticResource NotNullToVis}}"
                           Margin="0,12,0,0" Opacity="0.75" FontSize="12" TextWrapping="Wrap"/>
            </StackPanel>
        </Border>

        <Border Grid.Row="1"
                Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                CornerRadius="10" Padding="0"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto"/>
                    <RowDefinition Height="*"/>
                </Grid.RowDefinitions>
                <TextBlock Grid.Row="0" Text="이벤트 로그" FontSize="12" Opacity="0.7"
                           Margin="20,16,20,8"/>
                <ListBox Grid.Row="1"
                         ItemsSource="{Binding EventLog}"
                         BorderThickness="0"
                         Background="Transparent"
                         FontFamily="Consolas, Courier New"
                         FontSize="11"
                         Margin="12,0,12,12">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="6,2"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                </ListBox>
            </Grid>
        </Border>
    </Grid>
</UserControl>
