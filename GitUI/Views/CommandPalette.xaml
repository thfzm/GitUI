<UserControl x:Class="GitUI.Views.CommandPalette"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:ui="http://schemas.modernwpf.com/2019"
             xmlns:vm="clr-namespace:GitUI.ViewModels"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             d:DataContext="{d:DesignInstance Type=vm:CommandPaletteViewModel}"
             mc:Ignorable="d">
    <Grid Background="#88000000" MouseLeftButtonDown="Background_Click">
        <Border Width="600" MaxHeight="500"
                VerticalAlignment="Top" Margin="0,80,0,0"
                Background="{DynamicResource SystemControlBackgroundChromeMediumBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1"
                CornerRadius="10"
                MouseLeftButtonDown="Card_Click">
            <StackPanel>
                <TextBox x:Name="QueryBox"
                         Text="{Binding Query, UpdateSourceTrigger=PropertyChanged}"
                         ui:ControlHelper.PlaceholderText="🔍 리포 또는 명령 검색..."
                         FontSize="16"
                         Padding="16,12"
                         BorderThickness="0"
                         Background="Transparent"
                         KeyDown="QueryBox_KeyDown"/>
                <Border Height="1" Background="{DynamicResource SystemControlForegroundBaseLowBrush}"/>
                <ListBox x:Name="ResultsList"
                         ItemsSource="{Binding Results}"
                         SelectedIndex="{Binding SelectedIndex}"
                         BorderThickness="0"
                         Background="Transparent"
                         MaxHeight="400"
                         Focusable="False">
                    <ListBox.ItemContainerStyle>
                        <Style TargetType="ListBoxItem">
                            <Setter Property="Padding" Value="0"/>
                            <Setter Property="Margin" Value="6,2"/>
                            <Setter Property="HorizontalContentAlignment" Value="Stretch"/>
                        </Style>
                    </ListBox.ItemContainerStyle>
                    <ListBox.InputBindings>
                        <MouseBinding MouseAction="LeftClick"
                                      Command="{Binding ExecuteSelectedCommand}"/>
                    </ListBox.InputBindings>
                    <ListBox.ItemTemplate>
                        <DataTemplate>
                            <Border Padding="12,8" CornerRadius="6">
                                <Grid>
                                    <Grid.ColumnDefinitions>
                                        <ColumnDefinition Width="Auto"/>
                                        <ColumnDefinition Width="*"/>
                                        <ColumnDefinition Width="Auto"/>
                                    </Grid.ColumnDefinitions>
                                    <TextBlock Grid.Column="0" Text="{Binding Icon}" Margin="0,0,12,0" FontSize="16"/>
                                    <StackPanel Grid.Column="1">
                                        <TextBlock Text="{Binding Title}" FontWeight="SemiBold"/>
                                        <TextBlock Text="{Binding Subtitle}" Opacity="0.55" FontSize="11"
                                                   Visibility="{Binding Subtitle, Converter={StaticResource NotNullToVis}}"/>
                                    </StackPanel>
                                    <TextBlock Grid.Column="2" Text="{Binding Hint}" Opacity="0.4" FontSize="11"
                                               Visibility="{Binding Hint, Converter={StaticResource NotNullToVis}}"/>
                                </Grid>
                            </Border>
                        </DataTemplate>
                    </ListBox.ItemTemplate>
                </ListBox>
                <TextBlock Text="↑↓ 이동 · Enter 실행 · Esc 닫기"
                           Opacity="0.4" FontSize="10"
                           HorizontalAlignment="Center" Margin="0,4,0,8"/>
            </StackPanel>
        </Border>
    </Grid>
</UserControl>
