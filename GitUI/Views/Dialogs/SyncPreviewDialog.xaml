<Window x:Class="GitUI.Views.Dialogs.SyncPreviewDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:ui="http://schemas.modernwpf.com/2019"
        ui:WindowHelper.UseModernWindowStyle="True"
        Title="동기화 미리보기"
        Height="600" Width="720"
        WindowStartupLocation="CenterOwner"
        ShowInTaskbar="False">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <!-- Summary -->
        <StackPanel Grid.Row="0" Orientation="Horizontal" Margin="0,0,0,12">
            <Border Padding="10,6" CornerRadius="6" Background="#2D7D45" Margin="0,0,8,0">
                <TextBlock Foreground="White" FontWeight="SemiBold">
                    <Run Text="추가 "/><Run Text="{Binding Added, Mode=OneWay}"/>
                </TextBlock>
            </Border>
            <Border Padding="10,6" CornerRadius="6" Background="#A87929" Margin="0,0,8,0">
                <TextBlock Foreground="White" FontWeight="SemiBold">
                    <Run Text="수정 "/><Run Text="{Binding Modified, Mode=OneWay}"/>
                </TextBlock>
            </Border>
            <Border Padding="10,6" CornerRadius="6" Background="#B33232" Margin="0,0,8,0">
                <TextBlock Foreground="White" FontWeight="SemiBold">
                    <Run Text="삭제 "/><Run Text="{Binding Deleted, Mode=OneWay}"/>
                </TextBlock>
            </Border>
            <Border Padding="10,6" CornerRadius="6" Background="#3A3A3A" Margin="0,0,8,0">
                <TextBlock Foreground="White">
                    <Run Text="변경 없음 "/><Run Text="{Binding Unchanged, Mode=OneWay}"/>
                </TextBlock>
            </Border>
            <Border Padding="10,6" CornerRadius="6" Background="#5C2A2A">
                <TextBlock Foreground="White">
                    <Run Text="스킵 "/><Run Text="{Binding Skipped, Mode=OneWay}"/>
                </TextBlock>
            </Border>
        </StackPanel>

        <TextBlock Grid.Row="1" Margin="0,0,0,8" Opacity="0.7" FontSize="12">
            <Run Text="아래 파일들이 커밋 메시지 '"/><Run Text="{Binding Message, Mode=OneWay}"/><Run Text="'로 업로드됩니다."/>
        </TextBlock>

        <!-- File list -->
        <Border Grid.Row="2" Background="{DynamicResource SystemControlBackgroundChromeMediumLowBrush}"
                BorderBrush="{DynamicResource SystemControlForegroundBaseLowBrush}"
                BorderThickness="1" CornerRadius="8">
            <ListBox ItemsSource="{Binding Entries}"
                     BorderThickness="0" Background="Transparent"
                     ScrollViewer.HorizontalScrollBarVisibility="Disabled">
                <ListBox.ItemTemplate>
                    <DataTemplate>
                        <Grid Margin="6,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="Auto"/>
                                <ColumnDefinition Width="*"/>
                                <ColumnDefinition Width="Auto"/>
                            </Grid.ColumnDefinitions>
                            <Border Grid.Column="0" Padding="6,2" CornerRadius="4" Margin="0,0,8,0" VerticalAlignment="Center" MinWidth="48">
                                <Border.Style>
                                    <Style TargetType="Border">
                                        <Setter Property="Background" Value="#3A3A3A"/>
                                        <Style.Triggers>
                                            <DataTrigger Binding="{Binding Change}" Value="Added">
                                                <Setter Property="Background" Value="#2D7D45"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Change}" Value="Modified">
                                                <Setter Property="Background" Value="#A87929"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Change}" Value="Deleted">
                                                <Setter Property="Background" Value="#B33232"/>
                                            </DataTrigger>
                                            <DataTrigger Binding="{Binding Change}" Value="Skipped">
                                                <Setter Property="Background" Value="#5C2A2A"/>
                                            </DataTrigger>
                                        </Style.Triggers>
                                    </Style>
                                </Border.Style>
                                <TextBlock FontSize="10" FontWeight="SemiBold" Foreground="White"
                                           HorizontalAlignment="Center"
                                           Text="{Binding Change}"/>
                            </Border>
                            <TextBlock Grid.Column="1" Text="{Binding TargetPath}"
                                       FontFamily="Consolas, Courier New" FontSize="12"
                                       VerticalAlignment="Center" TextTrimming="CharacterEllipsis"/>
                            <TextBlock Grid.Column="2" Text="{Binding Reason}"
                                       Visibility="{Binding Reason, Converter={StaticResource NotNullToVis}}"
                                       Opacity="0.55" FontSize="11" Margin="8,0,0,0" VerticalAlignment="Center"/>
                        </Grid>
                    </DataTemplate>
                </ListBox.ItemTemplate>
            </ListBox>
        </Border>

        <!-- Buttons -->
        <Grid Grid.Row="3" Margin="0,16,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*"/>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="Auto"/>
            </Grid.ColumnDefinitions>
            <Button Grid.Column="1" Click="Cancel_Click" Content="취소" Width="100" Height="36" Margin="0,0,8,0"/>
            <Button Grid.Column="2" Click="Confirm_Click"
                    Style="{StaticResource AccentButtonStyle}"
                    Content="업로드 진행" Width="120" Height="36"/>
        </Grid>
    </Grid>
</Window>
